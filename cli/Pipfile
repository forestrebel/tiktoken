[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
typer = ">=0.9.0"
rich = ">=13.0.0"
pyyaml = ">=6.0.0"
python-dotenv = ">=1.0.0"
docker-compose = ">=1.29.2"
fastapi = ">=0.68.0"
uvicorn = ">=0.15.0"
aiohttp = ">=3.8.0"

[dev-packages]
pytest = ">=7.0.0"
black = ">=22.0.0"
isort = ">=5.0.0"
flake8 = ">=4.0.0"

[requires]
python_version = "3.12.4"

[scripts]
t = "python -m cli.cli" 